<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="yjc.wdb.scts.CustomerMapper">
  <select id="checkCustomer" resultType="int">
    select count(*) 
    from  customer 
    where customer_id = #{customer_id} and customer_pw= #{customer_pw}
  </select>
  
  <insert id="joinCustomer">
  insert into customer
  values(#{customer_id}, #{customer_pw});
  </insert>
  
  
  </mapper>